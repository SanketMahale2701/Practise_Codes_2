#include<stdio.h>
#include<stdlib.h>
struct Node
{
    int data;
    struct Node *next;  
};

typedef struct Node Node;
typedef struct Node* PNODE;
typedef struct Node** PPNODE;

int main()
{
    PNODE Frist = NULL;
    
    return 0;
}